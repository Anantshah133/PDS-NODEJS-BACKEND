// const { Router } = require("express");
// const { upload } = require("../utils/multer");
// const {
//   getAllSlider,
//   createSlider,
//   updateSlider,
//   deleteSlider,
// } = require("../controllers/slider");
// const { protect } = require("../middleware/protect");

// const router = Router();

// router
//   .get("/", getAllSlider)
//   .post("/", protect, upload.single("image"), createSlider)
//   .put("/", protect, upload.single("image"), updateSlider)
//   .delete("/:slider_id", protect, deleteSlider);

// module.exports = router;
